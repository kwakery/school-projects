#include <string>
using namespace std;

/* Prototypes */
int calcSegments(string);
void displaySplitTweet(string*, int, string);
int getLastPuncPos(string, int);
void splitTweets(string, string*, int*);
